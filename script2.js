console.log('script2.js');
console.log('we use "git status" to check status');
console.log('we use "git log" to check history');
console.log('we use "git commit" to commit the file into repo');
console.log('we use "git branch" to tell which branch we are on');
console.log('we use "git diff" to tell us the difference between the new saved file and earlier commited file');
console.log('we use "git checkout -b (branch-name)" to checkout from current branch and to the new branch');
console.log('if we want to switch from one branch to another already created branch we use "git checkout (branchname)"');

